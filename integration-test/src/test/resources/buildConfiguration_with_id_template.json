{
    "id": "${_id}",
    "name": "${_name}",
    "buildScript": "${_buildScript}",
    "repositoryConfiguration": {
        "id": ${_repositoryConfigurationId}
    },
    "lastModificationTime": ${_lastModificationTime},
    "project": {
      "id": ${_projectId}
    },
    "environment": {
      "id": ${_environmentId}
    }
}
