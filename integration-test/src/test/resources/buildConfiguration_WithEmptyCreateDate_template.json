{
    "name": "${_name}",
    "repositoryConfiguration": {
      "id": ${_repositoryConfigurationId}
    },
    "project": {
      "id": ${_projectId}
    },
    "buildType" : "MVN",
    "environment": {
      "id": ${_environmentId}
    }
}
