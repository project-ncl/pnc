{
    "name": "${_name}",
    "repositoryConfiguration": {
      "id": ${_repositoryConfigurationId}
    },
    "project": {
      "id": ${_projectId}
    },
    "environment": {
      "id": ${_environmentId}
    }
}
