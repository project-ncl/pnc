{
    "kind": "Route",
    "apiVersion": "v1",
    "metadata": {
        "name": "${pod-name}"
    },
    "spec": {
        "host" : "ncl-test-os-01.host.prod.eng.bos.redhat.com",
        "path" : "${pod-name}",
        "to" : {
            "kind" : "Service",
            "name" : "${service-name}"
        }
    }
}
