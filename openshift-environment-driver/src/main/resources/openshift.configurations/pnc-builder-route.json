{
    "kind": "Route",
    "apiVersion": "v1",
    "metadata": {
        "name": "${route-name}"
    },
    "spec": {
        "host" : "${build-agent-host}",
        "path" : "${route-path}",
        "to" : {
            "kind" : "Service",
            "name" : "${service-name}"
        }
    }
}
